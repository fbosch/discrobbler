language: node_js
yarn: true
node_js: "7"
cache:
  directories:
  - node_modules
before_script:
- yarn prune
script:
- yarn run build
- yarn test
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
  on:
    branch: master