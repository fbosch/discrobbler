language: node_js
yarn: true
node_js: "7"
cache:
  directories:
  - node_modules
before_deploy: 
  - chmod a+x deploy.sh
  - bash ./deploy.sh
deploy:
  provider: pages
  skip_cleanup: true
  local_dir: 'public'
  github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
  on:
    branch: master